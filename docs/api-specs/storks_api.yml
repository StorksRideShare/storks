openapi: 3.1.0
info:
  title: Storks API Umbrella
  version: 1.0.0
  description: Comprehensive entry point for all Storks microservices.

paths:
  /test:
    get:
      responses:
        "200":
          description: OK
  /health:
    get:
      description: API Gateway health check
      responses:
        "200":
          description: OK

# This file serves as a reference point. Each microservice has its own detailed spec.
# - admin-and-analytics.yml
# - booking-and-payment.yml
# - live-messaging.yml
# - location-and-navigation.yml
# - matching-intelligence.yml
# - safety-and-verification.yml
# - user-service.yml
# - shared.yml (Shared Components)
